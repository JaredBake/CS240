package chess;
// private variables for model
public class Result {// private variables for model
    // private variables for result
    private boolean login = false;// private variables for result
    private boolean user = false;// private variables for result
    private boolean register = false;// private variables for result
    private boolean join_game = false;// private variables for result
    // private variables for result
    boolean hasAccess(String s){// private variables for result

// private variables for result
        return login;
    }
}
